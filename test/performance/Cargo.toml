[package]
name = "search-filtering-performance"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
reqwest = { version = "0.11", features = ["json"] }
serde_json = "1.0"
fastrand = "2.0"

[dev-dependencies]
tokio-test = "0.4"
